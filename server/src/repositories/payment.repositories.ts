// Placeholder, if you want to save orders/payments in DB.

export const saveOrderToDB = async (order: any) => {
  // Save order info to your database here
};

export const savePaymentToDB = async (payment: any) => {
  // Save payment info to your database here
};
